# Allow packages with slightly mismatched peer deps to install.
# Required because @clerk/nextjs pins its React peer dep to ~19.2.3 while
# this project uses react@19.2.0.  Remove once Clerk widens its peer range.
legacy-peer-deps=true
